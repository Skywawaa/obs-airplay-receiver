name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup MSVC Developer Command Prompt
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install OpenSSL via Chocolatey
        run: choco install openssl -y --no-progress

      - name: Create deps directory
        shell: bash
        run: mkdir -p deps

      - name: Download OBS Studio SDK 32.1.1
        shell: bash
        run: |
          curl -L -o deps/obs-sdk.zip \
            "https://github.com/obsproject/obs-studio/archive/refs/tags/32.1.1.zip"
          mkdir -p deps/obs-sdk
          cd deps/obs-sdk
          unzip -q ../obs-sdk.zip

      - name: Create obsconfig.h for OBS SDK
        shell: bash
        run: |
          cat > deps/obs-sdk/obs-studio-32.1.1/libobs/obsconfig.h << 'EOF'
          #pragma once
          #define OBS_DATA_PATH "data"
          #define OBS_PLUGIN_PATH "obs-plugins/64bit"
          #define OBS_PLUGIN_DESTINATION "obs-plugins/64bit"
          #define OBS_RELEASE_CANDIDATE 0
          #define OBS_BETA 0
          EOF

      - name: Download FFmpeg 7.1 headers
        shell: bash
        run: |
          curl -L -o deps/ffmpeg7-headers.zip \
            "https://github.com/FFmpeg/FFmpeg/archive/refs/tags/n7.1.1.zip"
          cd deps
          unzip -q ffmpeg7-headers.zip
          mkdir -p ffmpeg7-include
          for lib in libavcodec libavutil libswscale libswresample; do
            mkdir -p ffmpeg7-include/$lib
            cp FFmpeg-n7.1.1/$lib/*.h ffmpeg7-include/$lib/
          done
          # Create required generated headers
          cat > ffmpeg7-include/libavutil/avconfig.h << 'AVEOF'
          #ifndef AVUTIL_AVCONFIG_H
          #define AVUTIL_AVCONFIG_H
          #define AV_HAVE_BIGENDIAN 0
          #define AV_HAVE_FAST_UNALIGNED 1
          #endif
          AVEOF
          cat > ffmpeg7-include/libavutil/ffversion.h << 'FFEOF'
          #ifndef AVUTIL_FFVERSION_H
          #define AVUTIL_FFVERSION_H
          #define FFMPEG_VERSION "7.1.1"
          #endif
          FFEOF

      - name: Download libplist 2.7.0
        shell: bash
        run: |
          curl -L -o deps/libplist.zip \
            "https://github.com/libimobiledevice/libplist/archive/refs/tags/2.7.0.zip"
          cd deps && unzip -q libplist.zip

      - name: Create libplist config.h
        shell: bash
        run: |
          cat > deps/libplist-2.7.0/config.h << 'EOF'
          #pragma once
          #define PACKAGE_VERSION "2.7.0"
          #define PACKAGE_NAME "libplist"
          #define PACKAGE_STRING "libplist 2.7.0"
          #define HAVE_STRDUP 1
          #define HAVE_STDINT_H 1
          #define HAVE_STDLIB_H 1
          #define HAVE_STRING_H 1
          #ifdef _MSC_VER
          #define strcasecmp _stricmp
          #define strncasecmp _strnicmp
          #define strdup _strdup
          #ifndef ssize_t
          #include <basetsd.h>
          typedef SSIZE_T ssize_t;
          #endif
          #endif
          EOF

      - name: Build libplist
        run: |
          cd deps\libplist-2.7.0
          if exist build rmdir /s /q build
          mkdir build
          cd build
          cmake .. -G "NMake Makefiles" -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=cl
          nmake

      - name: Generate import libraries
        shell: bash
        run: |
          # Generate stub .def files for OBS and dependencies
          # obs.lib
          cat > deps/obs.def << 'EOF'
          LIBRARY obs
          EXPORTS
              obs_register_source
              obs_source_output_video
              obs_source_output_audio
              obs_data_get_string
              obs_data_get_int
              obs_data_get_bool
              obs_data_set_default_string
              obs_data_set_default_int
              obs_data_set_default_bool
              obs_properties_create
              obs_properties_add_text
              obs_properties_add_int
              obs_properties_add_bool
              obs_properties_add_list
              obs_property_list_add_string
              obs_property_list_add_int
              obs_get_locale
              blog
              bzalloc
              bfree
          EOF
          lib //def:deps/obs.def //out:deps/obs.lib //machine:x64

          # w32-pthreads.lib
          cat > deps/w32-pthreads.def << 'EOF'
          LIBRARY w32-pthreads
          EXPORTS
              pthread_create
              pthread_join
              pthread_mutex_init
              pthread_mutex_destroy
              pthread_mutex_lock
              pthread_mutex_unlock
              pthread_cond_init
              pthread_cond_destroy
              pthread_cond_signal
              pthread_cond_broadcast
              pthread_cond_wait
              pthread_cond_timedwait
          EOF
          lib //def:deps/w32-pthreads.def //out:deps/w32-pthreads.lib //machine:x64

          # dnssd.lib (stub - UxPlay loads via LoadLibrary at runtime)
          cat > deps/dnssd.def << 'EOF'
          LIBRARY dnssd
          EXPORTS
              DNSServiceRegister
              DNSServiceRefDeallocate
              TXTRecordCreate
              TXTRecordDeallocate
              TXTRecordSetValue
              TXTRecordGetLength
              TXTRecordGetBytesPtr
          EOF
          lib //def:deps/dnssd.def //out:deps/dnssd.lib //machine:x64

      - name: Create FFmpeg stub libs
        shell: bash
        run: |
          mkdir -p deps/obs-ffmpeg-libs
          for name in avcodec avutil swscale swresample; do
            echo -e "LIBRARY ${name}\nEXPORTS\n    av_dummy_${name}" > deps/${name}.def
            lib //def:deps/${name}.def //out:deps/obs-ffmpeg-libs/${name}.lib //machine:x64
          done

      - name: Set OpenSSL path
        id: openssl
        shell: bash
        run: |
          OPENSSL_DIR="C:/Program Files/OpenSSL"
          if [ -d "C:/Program Files/OpenSSL-Win64" ]; then
            OPENSSL_DIR="C:/Program Files/OpenSSL-Win64"
          fi
          echo "dir=$OPENSSL_DIR" >> "$GITHUB_OUTPUT"
          echo "OpenSSL dir: $OPENSSL_DIR"

      - name: Build UxPlay libraries
        run: |
          cd deps\uxplay-build
          if exist build rmdir /s /q build
          mkdir build
          cd build
          cmake .. -G "NMake Makefiles" -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=cl -DOPENSSL_DIR="${{ steps.openssl.outputs.dir }}"
          nmake

      - name: Build plugin
        run: |
          if exist build rmdir /s /q build
          mkdir build
          cd build
          cmake .. -G "NMake Makefiles" -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=cl -DOPENSSL_DIR="${{ steps.openssl.outputs.dir }}"
          nmake

      - name: Prepare release artifact
        shell: bash
        run: |
          mkdir -p artifact
          cp build/obs-airplay-receiver.dll artifact/
          OPENSSL_DIR="${{ steps.openssl.outputs.dir }}"
          if [ -f "$OPENSSL_DIR/bin/libcrypto-3-x64.dll" ]; then
            cp "$OPENSSL_DIR/bin/libcrypto-3-x64.dll" artifact/
          elif [ -f "$OPENSSL_DIR/libcrypto-3-x64.dll" ]; then
            cp "$OPENSSL_DIR/libcrypto-3-x64.dll" artifact/
          fi
          cp install.bat artifact/
          cp uninstall.bat artifact/
          cp README.md artifact/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: obs-airplay-receiver
          path: artifact/
